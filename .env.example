# Optional: Hugging Face Inference API token (free tier)
# If left blank, the app automatically uses the local trained ML model instead.
# Get a free token at https://huggingface.co/settings/tokens
HF_API_TOKEN=
